# REASON: a backend nothing implements is the keeper:// case one layer over:
# written down, resolved by nothing.
name: box
scope: user
backend: ftp
summary: "x"
addresses: [box]
location:
  path: "~/objects"
holds:
  - class: recording
replicated: false
recovery:
  backed_up: false
