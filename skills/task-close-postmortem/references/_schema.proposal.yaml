# Proposal File Frontmatter Schema: pointer, not a copy.
#
# The one definition lives next to the proposals it validates:
#   work/_learning/_schema.proposal.yaml
# This file only refers to it, so a field is added in one place. Two
# hand-kept copies drifted before: a field could land in one and not the
# other. Validate against either path; both resolve to the same schema.

$schema: "https://json-schema.org/draft/2020-12/schema"
$ref: "../../../work/_learning/_schema.proposal.yaml"
